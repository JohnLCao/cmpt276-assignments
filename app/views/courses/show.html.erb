<p id="notice"><%= notice %></p>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @courses.each do |course| %>
      <tr>
        <td><%= course["student_id"]%>, <%= course["percentage"]%>, <%= course["lettergrade"]%></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Back', courses_path %>
<%= link_to 'Assign Grades', a2_histogram_path(@course)%>
